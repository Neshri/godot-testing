class_name SpringParameters
extends Resource

@export var stiffness: float = 20.0
@export var damping: float = 1.5

# Note: The 'length' and 'rest_length' properties are not used in the
# current simulation, but you can keep them for future use.
# @export var length: float = 10.0
# @export var rest_length: float = 5.0
